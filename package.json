{
  "name": "ramestta-agent-os-contracts",
  "version": "0.1.0",
  "description": "Ramestta AI Agent OS core contracts — Scheduler, AgentTreasury, AgentBootHelper (Workstream A)",
  "private": true,
  "scripts": {
    "build": "hardhat compile",
    "test": "hardhat test",
    "deploy:testnet:existing": "hardhat run scripts/deploy-existing-copies.js --network ramesttaTestnet"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "@openzeppelin/hardhat-upgrades": "^3.2.0",
    "dotenv": "^16.4.5",
    "hardhat": "^2.22.10"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.4.0",
    "@openzeppelin/contracts-upgradeable": "^5.4.0"
  }
}
